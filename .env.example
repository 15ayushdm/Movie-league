# Shared password gate (single password for ~10 league members)
LEAGUE_PASSWORD="change-me"

# Database (SQLite for local dev, swap to a Postgres URL in prod)
DATABASE_URL="file:./prisma/dev.db"

# Anthropic API key for the /scoring chat
ANTHROPIC_API_KEY="sk-ant-..."
